---
import { getCollection } from 'astro:content';

const notes = await getCollection('notes');
---
<h1>Notes</h1>
<ul>
  {notes.map((note) => (
    <li>
      <a href={`/notes/${note.slug}`}>{note.data.title}</a> - {note.data.date}
    </li>
  ))}
</ul>